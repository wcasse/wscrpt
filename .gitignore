# Build
/target/

# Local experiment lanes (never part of the release tree)
/scratch/
/autoresearch/

# Remote preview spike dependencies and evidence
/previewd/node_modules/
/output/preview-phase0/

# Native preview harness local build state
/clients/ipad-preview-harness/DerivedData/
/clients/ipad-preview-harness/build/
xcuserdata/
*.xcuserstate

# Provenance payloads: keep Markdown + checksum index; ignore binaries/crates
/dist/checkpoints/*
!/dist/checkpoints/*.md
!/dist/checkpoints/SHA256SUMS
!/dist/checkpoints/README.md

# Editor / OS noise
*.swp
*.tmp
*.log
.DS_Store
.idea/
.vscode/
*.crate

# Local MCP / tooling residue
unreal_mcp.log

# Private working notes (never publish)
/BIRDWORLD
/wscrpt JOB LIST
/wscrpt JOB LIST corrected
